apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: umap-backend-storage
  namespace: umap
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  storageClassName: standard
